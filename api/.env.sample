DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB=
PORT=
# JWT_SECRET must be at least 32 bytes (e.g. `openssl rand -base64 48`)
JWT_SECRET=
# Keep true outside local HTTP development. Secure cookies are sent over HTTPS only.
COOKIE_SECURE=true
